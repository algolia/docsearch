#!/usr/bin/env bash

set -ev # exit when error

npm run test:coverage
npm prune
NODE_ENV=production npm run build

if [ "$TRAVIS_PULL_REQUEST" == 'false' ] && [ "$TRAVIS_BRANCH" == 'v1' ]; then
  ./scripts/finish-release
fi
