language: node_js

script:
  - '${SCRIPT}'

env:
  matrix:
    - SCRIPT="yarn run lint"
    - SCRIPT="yarn run type-check"
    - SCRIPT="yarn test"

branches:
  only:
    - master

matrix:
  include:
    - name: '[Documentation] Deploy'
      script: './scripts/travis/deploy'
